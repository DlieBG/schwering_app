import { SafeResourceUrl } from "@angular/platform-browser";

export interface App {
    appId: string;
    name: string;
    shortName: string;
    description: string;
    icon: string;
    url: string;
    widgetUrl: string;
}
